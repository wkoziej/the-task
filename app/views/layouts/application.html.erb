<!DOCTYPE html>
<html>
<head>
  <title>TheTask</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div id="top_menu">      
    <%= link_to("Logout", destroy_user_session_path, :method => :delete) %>
  </div>

  <div id="menu">
    <%= link_to 'home', '/' %> | <%= link_to 'games', games_path %> | <%= link_to 'plays', plays_path %> | <%= link_to 'my profile', user_path(current_user) if current_user %> 
  </div>

  <div id="content"><%= content_for?(:content) ? yield(:content) : yield %></div>

<!--<%= yield %>-->

</body>
</html>
